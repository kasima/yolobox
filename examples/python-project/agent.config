# yolobox config for Python project

DEV_IMAGE=myproject-dev
BASE=origin/main

# Include project requirements in image build
BUILD_ARGS=--build-arg REQUIREMENTS_FILE=requirements.txt --build-arg INSTALL_NODE=false
